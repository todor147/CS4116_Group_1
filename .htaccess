<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /CS_4116_group_01/
    
    # Handle PHP files
    AddType application/x-httpd-php .php
    
    # Ensure PHP files are processed
    <FilesMatch "\.php$">
        SetHandler application/x-httpd-php
    </FilesMatch>
</IfModule> 